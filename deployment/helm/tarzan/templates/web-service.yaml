apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.web.service.name }}
  labels:
    {{- include "tarzan.labels" . | nindent 4 }}
    app.kubernetes.io/name: web
spec:
  type: {{ .Values.web.service.type }}
  selector:
    {{- include "tarzan.selectorLabels" (dict "component" "web" "root" .) | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.web.service.port }}
      targetPort: http
